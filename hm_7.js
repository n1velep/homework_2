console.log(null == 0); // false
console.log(null < 0); // false
console.log(null > 0); // false

/* Везде результат False, так как null это пустое значение, то есть там нет ничего.
Поэтому любой результат сравнения будет не верен, 0 не с чем сравнивать*/